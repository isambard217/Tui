import React from 'react';

// Components
import CaseForm from 'components/CaseFormComponent/CaseForm';

export default () => {
    return (
        <div>
            Im the app
            <CaseForm/>
        </div>

    );
};