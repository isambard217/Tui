import React from 'react';

// Components
import CaseForm from 'components/CaseForm';

export default () => {
    return (
        <div>
            Im the app
            <CaseForm/>
        </div>

    );
};